# 문서가 코드를 인용하는 방식

<!-- agctx:generated:status:start -->
**상태:** Implemented
<!-- agctx:generated:status:end -->

## 제안 요약

### 목적과 이유

| 항목 | 내용 |
| --- | --- |
| 제안 목표 | 문서가 코드를 복사하지 않고 가리키게 한다. `파일:줄` 인용과 코드 발췌를 없애고, 파일과 이름으로 가리킨 뒤 그 이름이 실제로 있는지 기계가 검사한다. |
| 제안 이유 | 지금은 문서가 코드를 세 가지 방식으로 복사한다. 줄 번호, 코드 발췌, 동작을 말로 다시 적은 문장이다. 복사본은 코드가 바뀔 때마다 따로 고쳐야 하므로 해시 게이트가 자주 울리고, 자주 울리면 사람이 읽지 않고 지문만 다시 찍는다. 그 결과 게이트가 통과하는 상태에서도 인용이 틀려 있다([측정](#측정)). |

### 범위와 우선순위

| 항목 | 내용 |
| --- | --- |
| 대상 계층 | `docs/contributing/implementation-mechanics.md`, `docs/contributing/implementation-principles.md`, `docs/contributing/doc-style.md`, `docs/contributing/doc-gate.md`, `tools/check-docs.ts`, `AGENTS.md`의 문서 규칙, `repo-docs` 스킬 |
| 결정할 것 | 인용 방식과 두 문서의 역할은 2026-09-20에 정했다([결정](#결정)). 남은 것은 이름으로 가리킬 수 없는 동작을 어떻게 가리킬지다. |
| 중요도 | High: 문서가 코드와 어긋나는 것을 막는 장치의 전제를 바꾼다. 패키지 동작에는 영향이 없다. |

### 의존성과 실행 순서

| 항목 | 내용 |
| --- | --- |
| 선행 작업 | 없음 |
| 선행 제안 | 없음 |
| 후속 제안 | [문서 소스 해시 게이트의 핀 범위와 승인 단위](doc-gate-pin-scope.md). 인용 방식을 바꾸면 게이트가 울릴 이유가 줄어드는 만큼, 그 제안의 범위를 다시 정한다. |
| 연관 제안 | [문서 정확성 자동 리뷰](doc-accuracy-review.md) |
| 후속 작업 | 바꾼 뒤 같은 방법으로 틀린 인용 수와 재기록 때 고친 줄의 성격을 다시 잰다. |
| 권장 다음 작업 | 없음. 남은 것은 이름으로 가리킬 수 없는 동작을 어떻게 가리킬지 정하는 일이며, [결정·검증 항목](#결정검증-항목)에 있다. |

## 목차

- [현재 동작](#현재-동작)
- [측정](#측정)
- [제안 동작](#제안-동작)
- [검토한 대안](#검토한-대안)
- [결정](#결정)
- [결정·검증 항목](#결정검증-항목)
- [구현 기록](#구현-기록)

## 현재 동작

문서는 코드를 세 가지 방식으로 복사한다.

```text
① 줄 번호      `tools/generate-skills.ts:25-28`
② 코드 발췌    문서 안의 ```ts 블록
③ 동작 설명    "요약은 영어 메시지 카탈로그에서 가져온다"
```

[문서 소스 해시 게이트](../../../contributing/doc-gate.md#문서-소스-해시-게이트)가 이 복사본을 지킨다. 인용한 소스가 바뀌면 문서를 다시 읽으라고 알리지만, 다시 읽었는지는 검사하지 못한다.

## 측정

2026-09-19에 잰 값이다. 대상은 인용이 몰린 두 문서이고, 방법은 아래에 적는다.

| 항목 | 값 | 방법 |
| --- | --- | --- |
| `파일:줄` 인용 | 311개(`implementation-mechanics.md` 210, `implementation-principles.md` 101) | 두 문서에서 정규식으로 셈 |
| 코드 발췌 블록 | 45개(각각 39, 6) | 코드 울타리 수를 2로 나눔 |
| 지금 틀린 인용 | 이름이 함께 적힌 인용 115개 중 10개 | 인용한 줄 범위 안에 그 이름이 있는지 확인 |
| 지문만 다시 찍은 경우 | 재기록 31번 중 10번 | 두 문서를 고친 커밋에서 지문 줄만 바뀐 것을 셈 |
| 숫자만 바꾼 줄 | 고친 133줄 중 51줄(38%) | 줄 번호를 `:N`으로 바꾸면 같아지는 줄을 셈 |

한 절에서 셋이 한꺼번에 틀어진 사례가 있다. `implementation-mechanics.md`의 22절은 PR #63과 #64 뒤에 코드 발췌(`commandList(ids …)`), 줄 번호 세 개, "영어 메시지 카탈로그" 설명이 모두 옛 상태로 남았다. 두 PR 모두 게이트가 울렸으나 문서에서는 지문 줄만 바뀌었다.

같은 내용을 코드 옆 주석에 적은 쪽은 맞는 상태로 남았다. 코드를 고친 변경에서 주석도 함께 고쳐졌기 때문이다. 외부 근거는 [문서와 코드의 드리프트 검출 근거](../../../references.md#문서와-코드의-드리프트-검출-근거)에 있다.

## 제안 동작

문서의 내용을 종류로 나누고, 종류마다 사람이 다시 읽지 않아도 맞는지 확인되는 방식으로 바꾼다.

| 내용 종류 | 바꾼 뒤 | 맞는지 확인하는 방법 |
| --- | --- | --- |
| 코드 위치 | `tools/generate-skills.ts`의 `SKILLS` | 그 이름이 그 파일에 있는지 `check:docs`가 검사 |
| 코드 발췌 | 없애고 위치만 가리킨다 | 확인할 것이 없다 |
| 코드에서 뽑을 수 있는 사실 | 생성하거나, 생성된 산출물을 가리킨다 | 생성 결과와 대조하는 평가 |
| 동작의 이유 | 코드 옆 주석이나 ADR에 한 번만 적는다 | 코드와 같은 변경에서 고쳐진다 |
| 말로 적은 동작 설명 | 꼭 필요한 것만 남긴다 | 절 단위 해시 게이트 |

22절을 예로 들면 이렇게 바뀐다.

```markdown
## 22. 에이전트용 스킬 생성

스킬 파일의 명령 목록은 사람이 쓰지 않고 명령 등록부에서 생성한다.
어느 명령이 어느 스킬에 들어가는지는 명령의 에이전트 정책이 정한다(ADR 0029).

- 스킬 목록: `tools/generate-skills.ts`의 `SKILLS`
- 표지 사이만 바꾸는 함수: 같은 파일의 `renderSkill`
- 목록이 최신인지 검사: `evals/skills.test.ts`
```

## 검토한 대안

| 대안 | 내용 | 판단 |
| --- | --- | --- |
| 지금 유지 | `파일:줄` 인용과 발췌를 두고 게이트만 절 단위로 개편한다. | 채택하지 않는다. 경보는 좁아지지만 복사본은 그대로라, 코드 위에 줄이 하나만 생겨도 그 아래를 인용한 절이 계속 걸린다. |
| 줄 번호만 이름으로 | 줄 번호를 이름으로 바꾸고 발췌와 설명은 둔다. | 채택하지 않는다. 숫자 맞추기는 없어지지만 발췌와 설명이 틀어지는 일은 남고, 그것을 잡는 방법은 여전히 사람이 다시 읽는 것뿐이다. |
| fiberplane/drift 도입 | 문서를 심볼에 묶고 정규화한 AST 지문으로 검사한다([근거](../../../references.md#문서와-코드의-드리프트-검출-근거)). | 채택하지 않는다. 지문에 위치가 없어 줄 번호가 밀리는 변화를 놓치고, 인용을 이름으로 바꾸고 나면 이름이 있는지만 확인하면 되므로 별도 실행 파일을 들일 이유가 없다. |

## 결정

2026-09-20, 제품 소유자.

- **복사하지 않고 가리킨다.** `파일:줄` 인용을 `파일`과 `이름`으로 바꾸고, 그 이름이 파일에 있는지 검사한다. 코드 발췌는 없애고, 코드에서 뽑을 수 있는 사실은 생성하거나 생성된 산출물을 가리키고, 이유는 코드 옆 주석과 ADR에 둔다.
- **두 문서의 역할을 나눈다.** `implementation-principles.md`는 npm·Node.js·CLI의 원리를 배우는 학습용 문서로 남기고 설명을 유지하되 인용만 이름으로 바꾼다. `implementation-mechanics.md`는 기능마다 "어느 파일의 어느 이름에 있고 이유는 어느 ADR에 있는지"를 알려 주는 안내도로 줄인다.
- 이 결정은 [핀 범위와 승인 단위](doc-gate-pin-scope.md)의 "`파일:줄` 인용 규칙은 유지한다"를 대체한다.

## 결정·검증 항목

- **인용 형식:** 결정했다. `파일`과 `이름`으로 가리키고 줄 번호를 쓰지 않는다.
- **두 문서의 역할:** 결정했다. 학습용과 안내도로 나눈다.
- **이름이 없는 동작:** 함수 안의 특정 분기처럼 이름으로 가리킬 수 없는 동작을 어떻게 가리킬지는 정하지 않았다. 그 동작을 보장하는 평가의 이름으로 가리키는 방법이 후보다.
- **가리킨 코드의 변경 검출:** 2026-09-20에 정했다. 인용마다 지문을 기록한다([ADR 0033](../../../adr/0033-fingerprint-cited-code.md)).
- **검증:** 바꾼 뒤 [측정](#측정)의 다섯 항목을 같은 방법으로 다시 잰다.

## 구현 기록

#### 구현 기록: 인용을 이름으로 바꾸고 두 문서를 다시 씀

**2026-09-20.**

* **결정:** [결정](#결정) 절의 내용대로 구현했고, 되돌리기 어려운 부분은 [ADR 0032](../../../adr/0032-cite-code-by-name.md)로 확정했다.
* **구현:** `tools/doc-citations.ts`가 인용 형식을 정하고, `tools/check-docs.ts`의 `checkCitations`가 문서마다 검사한다. 저장소가 소유한 파일을 줄 번호로 인용하면 실패하고, 가리킨 이름이 그 파일에 없으면 실패한다. `implementation-mechanics.md`는 기능마다 위치·이유·검사를 가리키는 안내도로 다시 썼고 소스 해시 마커를 뗐다. `implementation-principles.md`는 설명을 유지한 채 인용만 이름으로 바꿨다. 다른 문서 7곳에 남아 있던 줄 번호 인용 8개도 함께 고쳤다. 규칙은 `AGENTS.md`, `doc-style.md`, `doc-gate.md`, `repo-docs` 스킬에 적었다.
* **평가:** `evals/doc-citations.test.ts`의 5개 평가가 줄 번호 인용 금지, 이름 인용 해석, 검사 대상(저장소 소유 파일)과 제외 대상(이력 문서), 두 문서의 인용이 실제 이름을 가리키는지를 검사한다.
* **재측정:** [측정](#측정)과 같은 방법으로 다시 쟀다. `파일:줄` 인용은 311개에서 0개, 코드 발췌는 45개에서 6개(모두 명령 출력 예시이며 코드 복사는 0개), `implementation-mechanics.md`는 901줄에서 291줄이 됐다. 틀린 인용 10개는 모두 고쳐졌고, 같은 일이 생기면 `check:docs`가 막는다. 대신 기계가 검사하는 이름 인용 217개가 생겼다.
* **계획과 달라진 점:** 다른 문서에도 줄 번호 인용이 8개 남아 있었다. 제안은 두 문서만 대상으로 적었지만, 검사를 저장소 전체에 적용하므로 함께 고쳤다.
* **제약:** 이름으로 가리킬 수 없는 동작을 평가 이름으로 가리키는 방법은 아직 규칙으로 정하지 않았다. `docs/discussion/`·`docs/adr/`·`CHANGELOG.md`는 검사 대상이 아니므로 그 안의 옛 인용은 그대로 남는다.
* **다음 단계:** [핀 범위와 승인 단위](doc-gate-pin-scope.md) 개편의 범위를 이 결정 뒤의 상태로 다시 정한다.

#### 구현 기록: 인용한 코드의 지문으로 변경 검출

**2026-09-20.**

* **결정:** 이름 존재만 검사하면 가리킨 함수의 내용이 바뀌어도 알려 주지 못한다는 것이 드러나, 인용마다 지문을 기록하기로 했다([ADR 0033](../../../adr/0033-fingerprint-cited-code.md)).
* **구현:** `tools/symbol-source.ts`의 `citedText`가 대상을 잘라 내고 `symbolDigest`가 지문을 만든다. TypeScript는 최상위 선언, JSON은 키의 값, YAML은 키의 블록이며 Markdown은 지문을 붙이지 않는다. `tools/doc-citations.ts`의 `applyCitationMarkers`·`citationMarkerProblems`가 문서의 지문 주석을 쓰고 대조하며, `node tools/check-docs.ts --stamp`가 갱신한다. 코드 블록 안의 예시는 건드리지 않는다.
* **평가:** `evals/symbol-source.test.ts` 4개와 `evals/doc-citations.test.ts` 8개. 잘라 내기·지문·형식 검사와 함께, 문서가 인용한 이름을 모두 잘라 낼 수 있는지도 검사한다.
* **측정:** 인용 212곳에 지문이 기록됐다. `src/check.ts`의 `checkProject`에 주석 한 줄을 넣어 보니 지문 검사는 해당 문서의 항목 두 곳만 지목했고, 같은 변경에 소스 해시 게이트는 문서 7개를 걸었다.
* **drift와 비교한 측정(2026-09-20):** 최근 60커밋에서 인용한 심볼의 변경 295건을 놓고, 우리 지문(원문 그대로)과 drift식 정규화 지문(공백·주석 제외)이 각각 몇 번 울렸는지 셌다. 둘 다 74건이었고 우리만 울린 사례는 없었다. 이 저장소에 포매터가 없어 공백만 바꾸는 변경이 없었기 때문이며, 포매터를 도입하면 차이가 생기므로 그때 정규화를 검토한다. 설정 파일에서는 반대 방향의 차이가 있었다. `package.json`은 60커밋에서 5번 바뀌었고 인용한 키가 바뀐 것은 4번이라 우리는 1번을 울리지 않았는데, drift는 JSON을 지원 언어 밖으로 두고 원문 전체를 비교한다([근거](../../../references.md#문서와-코드의-드리프트-검출-근거)). `check:docs` 전체 실행은 0.30~0.35초다. drift는 설치하지 않아 실행 시간을 비교하지 못했다.
* **잘라 내기 품질:** TypeScript 인용 185곳을 모두 잘라 냈고 범위도 정확했다. 괄호 짝 검사에서 4건이 걸렸으나 정규식 리터럴 때문에 생긴 검사기의 오탐이었다.
* **계획과 달라진 점:** 제안에는 지문이 없었다. [제안 동작](#제안-동작)의 "코드 위치"는 이름 존재 확인까지만 적었는데, 그것만으로는 내용 변경을 놓치므로 지문을 더했다.
* **제약:** 얕은 파서는 최상위 선언과 들여쓰기로 구분되는 키에 기댄다. 그 밖의 형태를 인용하면 평가가 실패한다. TypeScript 7에는 JavaScript 파서 API가 없어 정확한 AST를 쓰려면 새 의존성이 필요하다.
* **다음 단계:** 남은 서술 문장을 지키는 소스 해시 게이트의 범위를 [핀 범위와 승인 단위](doc-gate-pin-scope.md)에서 다시 정한다.

#### 구현 기록: 지문 검사의 구멍 두 개를 막음

**2026-09-20.**

* **결정:** drift와 비교하며 나온 두 약점(얕은 파서의 범위, 정규화 없는 지문)을 재 보고, 실제로 필요한 두 가지만 고쳤다.
* **구현:** 인용한 이름이 최상위 선언이나 키가 아니면 `check:docs`가 실패시킨다. 전에는 이름이 파일 어딘가에 있기만 하면 통과했고 지문도 붙지 않았다. 그리고 지문을 계산하기 전에 줄바꿈을 LF로 맞춰, CRLF로 체크아웃한 컴퓨터에서도 같은 값이 나오게 했다.
* **평가:** `evals/symbol-source.test.ts`에 두 평가를 더했다. 함수 안의 지역 이름은 잘라 낼 수 없다는 것과, CRLF와 LF의 지문이 같다는 것이다.
* **측정과 판단:** 지문 없이 통과하던 인용은 현재 0개였다. 그래도 규칙으로 막지 않으면 다시 생기므로 검사를 더했다. 깊은 위치(클래스 메서드 등) 지원은 넣지 않았다. 저장소에 클래스 선언이 있는 파일은 1개이고 들여쓴 함수 선언은 0개라 인용할 대상이 없으며, 어기면 위 검사가 실패시킨다. 주석을 지문에서 빼는 정규화도 넣지 않았다. 이 저장소는 코드 옆 주석을 이유의 정본으로 삼으므로, 주석이 바뀌면 문서를 다시 읽는 편이 맞다. 공백 정규화는 포매터를 도입할 때 다시 본다.
* **제약:** 지문은 여전히 원문 그대로라 들여쓰기만 바뀌어도 울린다. `.gitattributes`가 `* text=auto eol=lf`를 정하고 있어 줄바꿈 차이는 원래도 드물었지만, 이제는 그 설정에 기대지 않는다.
* **다음 단계:** 없음.
